description: |-
  Тип места хранения:
  - NORMAL - обычный склад;
  - ENTRY_POINT - точка входа (приёмка);
  - EXIT_POINT - точка выхода (передача клиенту);
type: string
enum:
  - NORMAL
  - ENTRY_POINT
  - EXIT_POINT
example: "ENTRY_POINT"