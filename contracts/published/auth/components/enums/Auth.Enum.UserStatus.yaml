description: |-
  Текущий статус пользователя:
  - DRAFT - черновик;
  - ENABLED - используется;
  - DISABLED - отключён (админом);
  - BLOCKED - заблокирован (системой);
type: string
enum:
  - DRAFT
  - ENABLED
  - DISABLED
  - BLOCKED
example: "ENABLED"
