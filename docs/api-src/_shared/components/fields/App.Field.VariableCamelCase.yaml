App.Field.VariableCamelCase:
  description: |
    Переменная (camelCase + CamelCase)
    - pattern: "^[a-zA-Z][a-zA-Z0-9]+$"
  type: string
  # pattern: "^[a-zA-Z][a-zA-Z0-9]+$"
  minLength: 4
  maxLength: 32
  example: "goodVar001"